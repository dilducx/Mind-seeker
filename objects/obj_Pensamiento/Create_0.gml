	// Referencia al jugador
target = obj_jugador; 

// Rango de visión
vision_range = 200;

// Estado (0 = esperando, 1 = lanzado)
state = 0;

// Velocidad de ataque
attack_speed = 6;

// Tiempo de vida después del ataque
lifetime = 60; // 1 segundo (60 fps)
